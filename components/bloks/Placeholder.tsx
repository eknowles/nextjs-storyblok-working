import React from 'react';

const Placeholder: React.FC<{ componentName: string }> = ({ componentName }) => (
  <div>The component {componentName} has not been created yet.</div>
);

export default Placeholder;
